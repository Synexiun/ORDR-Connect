# ============================================================================
# ORDR-Connect — Developer Portal NetworkPolicy
# SOC 2 Type II | ISO 27001:2022 | HIPAA Compliant
# Developer portal: receive from ingress, can only call API
# ============================================================================
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: developer-portal-network-policy
  namespace: ordr-system
  labels:
    app.kubernetes.io/name: developer-portal
    app.kubernetes.io/component: developer-portal
    app.kubernetes.io/part-of: ordr-connect
    app.kubernetes.io/managed-by: kustomize
spec:
  podSelector:
    matchLabels:
      app.kubernetes.io/name: developer-portal
  policyTypes:
    - Ingress
    - Egress
  ingress:
    # From Istio ingress gateway
    - from:
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: istio-system
      ports:
        - protocol: TCP
          port: 3000
    # Prometheus scraping
    - from:
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: monitoring
      ports:
        - protocol: TCP
          port: 9090
  egress:
    # Only call API service
    - to:
        - podSelector:
            matchLabels:
              app.kubernetes.io/name: api
      ports:
        - protocol: TCP
          port: 3000
    # DNS
    - to: []
      ports:
        - protocol: UDP
          port: 53
        - protocol: TCP
          port: 53
